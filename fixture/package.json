{
  "name": "kintsugi-fixture",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "A deliberately-broken package the Kintsugi loop repairs.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit --noUnusedLocals",
    "test": "tsx --test --test-reporter=tap test/*.test.ts",
    "check:version": "node checks/version-check.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
